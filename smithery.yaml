# smithery.yaml — for a Python MCP server using stdio
version: 1

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    # no configuration parameters required
  commandFunction: |-
    (config) => ({
      command: "python",
      args: ["server.py"],
      env: {}
    })
